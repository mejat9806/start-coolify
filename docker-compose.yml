name: PullPreview
on:
  schedule:
    - cron: '30 */4 * * *'
  pull_request:
    types: [labeled, unlabeled, synchronize, closed, reopened, opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  deploy:
    if: github.event_name == 'schedule' || github.event.label.name == 'pullpreview' || contains(github.event.pull_request.labels.*.name, 'pullpreview')
    runs-on: ubuntu-slim
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      # 1. This step puts the keys into the runner's memory "silently"
      # so PullPreview can find them without seeing the "${{ }}" syntax.
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: $${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: $${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      # 2. Run PullPreview WITHOUT any 'env:' block.
      - uses: pullpreview/action@v6
        with:
          app_path: .
          deployment_target: compose
          admins: '@collaborators/push'
          instance_type: nano
          ttl: 1h
