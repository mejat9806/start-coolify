version: '3.8'
services:
  web:
    image: node:20-alpine
    ports:
      - '80:3000'
    working_dir: /app
    volumes:
      - .:/app
    command: node .output/server/index.mjs
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
